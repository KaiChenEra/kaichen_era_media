name: kaichen_era_media
description: Shared image picking, lossless WebP normalization, and sha256 utilities for KaiChenEra apps.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.5.0
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.2.1
  image_cropper: ^12.2.1
  image: ^4.9.1

  crypto: ^3.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
